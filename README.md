# ai-local-language-translate-chat-bot1
**AI Local Language Translator Chatbot** — An AI-powered chatbot that translates text between English and local/regional languages, making communication easier through a simple and interactive interface.
inside terminal write python -m venv venv
next type venv\scripts\activate
# Liberries 
pip install streamlit 
pip install langchain
pip install langchain-community
pip install langchain-google-genai
pip install google-generativeai
pip install faiss-cpu
pip install pypdf
pip install python-docx
pip install senetence-transformers
pip install python-dotenv
